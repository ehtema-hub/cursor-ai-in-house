[pytest]
pythonpath = .
testpaths = tests
addopts =
    --cov=app.blueprints.support
    --cov=app.support
    --cov=app.models.user
    --cov=app.blueprints.auth
    --cov-report=term-missing
    --cov-fail-under=84
